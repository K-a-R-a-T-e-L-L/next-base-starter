"use client";
import { getListTodos, useGetListTodos } from "@/shared/api";
import Image from "next/image";

export default function Home() {
  // const { data } = useGetListTodos();
  // console.log(data);

  // getListTodos();

  return (
    <>
      <>hello world</>
    </>
  );
}
